# dependencies (this project is dependency-free / no-build)
node_modules/
package-lock.json

# secrets — the Anthropic API key lives only in server/.env (never committed)
.env
*.env
!.env.example
server/.env

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# build output (none — static site served as-is)
dist/
